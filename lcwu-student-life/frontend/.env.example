# Base URL of the PHP backend API.
# Leave as /api to use the Vite dev proxy defined in vite.config.js,
# or point it at a full URL once the backend is deployed, for example
# https://api.lcwu-studentlife.edu.pk/api
VITE_API_BASE_URL=/api
